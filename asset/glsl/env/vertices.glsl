
const vec3 CUBE_VERTICES[36] = vec3[](
    vec3(-1,  1, -1),
    vec3(-1, -1, -1),
    vec3( 1, -1, -1),
    vec3( 1, -1, -1),
    vec3( 1,  1, -1),
    vec3(-1,  1, -1),

    vec3(-1, -1,  1),
    vec3(-1, -1, -1),
    vec3(-1,  1, -1),
    vec3(-1,  1, -1),
    vec3(-1,  1,  1),
    vec3(-1, -1,  1),

    vec3( 1, -1, -1),
    vec3( 1, -1,  1),
    vec3( 1,  1,  1),
    vec3( 1,  1,  1),
    vec3( 1,  1, -1),
    vec3( 1, -1, -1),

    vec3(-1, -1,  1),
    vec3(-1,  1,  1),
    vec3( 1,  1,  1),
    vec3( 1,  1,  1),
    vec3( 1, -1,  1),
    vec3(-1, -1,  1),

    vec3(-1,  1, -1),
    vec3( 1,  1, -1),
    vec3( 1,  1,  1),
    vec3( 1,  1,  1),
    vec3(-1,  1,  1),
    vec3(-1,  1, -1),

    vec3(-1, -1, -1),
    vec3(-1, -1,  1),
    vec3( 1, -1, -1),
    vec3( 1, -1, -1),
    vec3(-1, -1,  1),
    vec3( 1, -1,  1)
);
